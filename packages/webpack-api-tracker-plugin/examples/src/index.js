// Simple example application
console.log('Hello, webpack-api-tracker-plugin!');

// This is a simple example to demonstrate the plugin
document.addEventListener('DOMContentLoaded', () => {
  const app = document.getElementById('app');
  if (app) {
    app.innerHTML = '<h1>API Tracker Plugin Example</h1>';
  }
});