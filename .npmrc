# 自动安装 peerDependencies，防止团队成员遇到依赖缺失报错
auto-install-peers=true

# 提升依赖安装的兼容性
strict-peer-dependencies=false

# --- VitePress Hoisting Fix ---
# Vite/VitePress's optimizer expects some dependencies (especially from plugins
# like mermaid) to be available at the root level. This hoisting pattern
# makes them discoverable without polluting the root package.json.
public-hoist-pattern[]=*vitepress*
public-hoist-pattern[]=*mermaid*
public-hoist-pattern[]=*dayjs*
public-hoist-pattern[]=*cytoscape*
public-hoist-pattern[]=@braintree/sanitize-url
public-hoist-pattern[]=debug